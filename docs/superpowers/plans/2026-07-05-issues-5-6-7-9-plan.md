# Issue 5/6/7/9 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 解决 GitHub issues #5、#6、#7、#9：逐层 LRD 开关、统一 sigma_noise、lambda 初始化生效、支持 resume 训练。

**Architecture:** 通过扩展 `LRDConfig` 与 `TargetNet._should_use_lrd` 实现逐层控制；统一默认超参并打通 YAML 到 `MappingLoss` 的传参；扩展 checkpoint 保存/加载字段，在训练脚本和 trainer 中支持 resume。

**Tech Stack:** Python 3.13, PyTorch, uv, pytest, Ruff

## Global Constraints

- 所有可执行脚本使用 `uv run python3 ...`
- 代码风格：Ruff，行宽 100，单引号
- 不得破坏现有 checkpoint 兼容性；旧的 checkpoint 可以忽略 resume，但加载评估仍需可用
- 目标网络参数不得通过 `.data.copy_()` 注入，必须通过 functional forward

---

### Task 1: 扩展 LRDConfig 支持逐层开关

**Files:**
- Modify: `mapping_network/target_nets/lrd_config.py`
- Test: `tests/test_lrd_config.py`

**Interfaces:**
- `LRDConfig` 新增字段 `layer_enabled: dict`，支持字符串 `'true'`/`'false'`/`'auto'` 归一化为布尔/字符串。

- [ ] **Step 1: 写失败测试**

```python
def test_lrd_config_layer_enabled():
    cfg = LRDConfig(layer_enabled={'fc1': False, 'fc2': 'true'})
    assert cfg.layer_enabled == {'fc1': False, 'fc2': True}


def test_lrd_config_invalid_layer_enabled():
    with pytest.raises(ValueError):
        LRDConfig(layer_enabled={'fc1': 'invalid'})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `uv run python3 -m pytest tests/test_lrd_config.py -v`
Expected: FAIL（`layer_enabled` 不存在或值未归一化）

- [ ] **Step 3: 实现 LRDConfig 扩展**

```python
from dataclasses import dataclass, field


def _normalize_enabled(value):
    if isinstance(value, bool):
        return value
    if isinstance(value, str):
        lowered = value.lower()
        if lowered in ('true', '1', 'yes'):
            return True
        if lowered in ('false', '0', 'no'):
            return False
        if lowered in ('auto', ''):
            return 'auto'
    raise ValueError(f'Invalid LRD enabled value: {value!r}')


@dataclass
class LRDConfig:
    enabled: bool | str = 'auto'
    default_rank: int = 10
    layer_ranks: dict = field(default_factory=dict)
    layer_enabled: dict = field(default_factory=dict)
    auto_enable_threshold: int = 200_000

    def __post_init__(self):
        self.enabled = _normalize_enabled(self.enabled)
        normalized = {}
        for key, value in self.layer_enabled.items():
            normalized[key] = _normalize_enabled(value)
        self.layer_enabled = normalized
```

- [ ] **Step 4: 运行测试确认通过**

Run: `uv run python3 -m pytest tests/test_lrd_config.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add mapping_network/target_nets/lrd_config.py tests/test_lrd_config.py
git commit -m "feat: add per-layer LRD enabled config"
```

---

### Task 2: TargetNet 按优先级判断逐层 LRD

**Files:**
- Modify: `mapping_network/target_nets/base.py`
- Test: `tests/test_target_nets.py`

**Interfaces:**
- `TargetNet._should_use_lrd(self, layer_name: str, total_params: int) -> bool` 读取 `self._lrd_config.layer_enabled`。

- [ ] **Step 1: 写失败测试**

```python
def test_cnn2_per_layer_lrd_override():
    target = CNN2(lrd_config={'enabled': True, 'layer_enabled': {'fc2': False}})
    for s in target.get_param_slices():
        name = s.name if s.kind == 'full' else s.weight_name
        if name.startswith('fc2'):
            assert s.kind == 'full'
```

- [ ] **Step 2: 运行测试确认失败**

Run: `uv run python3 -m pytest tests/test_target_nets.py::test_cnn2_per_layer_lrd_override -v`
Expected: FAIL（`fc2` 仍被 LRD）

- [ ] **Step 3: 实现优先级判断**

```python
def _should_use_lrd(self, layer_name: str, total_params: int) -> bool:
    layer_enabled = self._lrd_config.layer_enabled.get(layer_name)
    if layer_enabled is not None:
        if layer_enabled is True:
            return True
        if layer_enabled is False:
            return False
    enabled = self._lrd_config.enabled
    if enabled is True:
        return True
    if enabled is False:
        return False
    return total_params > self._lrd_config.auto_enable_threshold
```

- [ ] **Step 4: 运行测试确认通过**

Run: `uv run python3 -m pytest tests/test_target_nets.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add mapping_network/target_nets/base.py tests/test_target_nets.py
git commit -m "feat: per-layer LRD enable/disable with priority"
```

---

### Task 3: train.py 合并 LWT 的 layer_generators lrd 配置

**Files:**
- Modify: `mapping_network/scripts/train.py`
- Test: `tests/test_configs.py`

**Interfaces:**
- LWT 场景下，`lrd_config` 合并 `layer_generators` 中的 `lrd_enabled` 和 `lrd_rank`。

- [ ] **Step 1: 修改 train.py 合并逻辑**

在构造 `lrd_config` 后、调用 `build_target_net` 前，加入：

```python
if cfg['training_strategy'] == 'lwt':
    layer_ranks = {}
    layer_enabled = {}
    for name, gen_cfg in cfg['layer_generators'].items():
        if 'lrd_rank' in gen_cfg:
            layer_ranks[name] = gen_cfg['lrd_rank']
        if 'lrd_enabled' in gen_cfg:
            layer_enabled[name] = gen_cfg['lrd_enabled']
    lrd_config = {
        **lrd_config,
        'layer_ranks': {**lrd_config.get('layer_ranks', {}), **layer_ranks},
        'layer_enabled': {**lrd_config.get('layer_enabled', {}), **layer_enabled},
    }
```

- [ ] **Step 2: 更新测试验证 LWT 层 LRD 配置生效**

在 `tests/test_configs.py` 中为 LWT 配置断言 `target_net` 的 LRD 切片符合预期（可选）。

- [ ] **Step 3: 运行测试**

Run: `uv run python3 -m pytest tests/test_configs.py -v`
Expected: PASS

- [ ] **Step 4: Commit**

```bash
git add mapping_network/scripts/train.py tests/test_configs.py
git commit -m "feat: merge LWT layer lrd config into global LRDConfig"
```

---

### Task 4: MappingLoss 接收 lambda_*_init

**Files:**
- Modify: `mapping_network/mapping/loss.py`
- Test: `tests/test_loss.py`

**Interfaces:**
- `MappingLoss.__init__(self, sigma_noise: float = 0.0001, lambda_st_init: float = 0.1, lambda_sm_init: float = 0.1, lambda_al_init: float = 0.1)`

- [ ] **Step 1: 写失败测试**

```python
def test_mapping_loss_lambda_inits():
    loss_fn = MappingLoss(lambda_st_init=0.01, lambda_sm_init=0.02, lambda_al_init=0.03)
    assert loss_fn.lambda_st.item() == 0.01
    assert loss_fn.lambda_sm.item() == 0.02
    assert loss_fn.lambda_al.item() == 0.03
```

- [ ] **Step 2: 运行测试确认失败**

Run: `uv run python3 -m pytest tests/test_loss.py::test_mapping_loss_lambda_inits -v`
Expected: FAIL

- [ ] **Step 3: 实现 MappingLoss 参数**

```python
def __init__(
    self,
    sigma_noise: float = 0.0001,
    lambda_st_init: float = 0.1,
    lambda_sm_init: float = 0.1,
    lambda_al_init: float = 0.1,
):
    super().__init__()
    self.sigma_noise = sigma_noise
    self.lambda_st = nn.Parameter(torch.tensor(lambda_st_init))
    self.lambda_sm = nn.Parameter(torch.tensor(lambda_sm_init))
    self.lambda_al = nn.Parameter(torch.tensor(lambda_al_init))
```

- [ ] **Step 4: 运行测试确认通过**

Run: `uv run python3 -m pytest tests/test_loss.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add mapping_network/mapping/loss.py tests/test_loss.py
git commit -m "feat: make MappingLoss lambda inits configurable"
```

---

### Task 5: train.py 从 YAML 读取 lambda_*_init

**Files:**
- Modify: `mapping_network/scripts/train.py`

**Interfaces:**
- `MappingLoss(sigma_noise=..., lambda_st_init=cfg.get(...), ...)`

- [ ] **Step 1: 修改 train.py**

```python
loss_fn = MappingLoss(
    sigma_noise=cfg.get('sigma_noise', 0.0001),
    lambda_st_init=cfg.get('lambda_st_init', 0.1),
    lambda_sm_init=cfg.get('lambda_sm_init', 0.1),
    lambda_al_init=cfg.get('lambda_al_init', 0.1),
).to(device)
```

- [ ] **Step 2: 运行 config 测试**

Run: `uv run python3 -m pytest tests/test_configs.py -v`
Expected: PASS

- [ ] **Step 3: Commit**

```bash
git add mapping_network/scripts/train.py
git commit -m "feat: read lambda inits from YAML config"
```

---

### Task 6: 统一 sigma_noise 默认值为 0.0001

**Files:**
- Modify: `configs/*.yaml`

**Interfaces:**
- 所有 mapping 配置 `sigma_noise: 0.0001`

- [ ] **Step 1: 修改所有 mapping YAML**

将 `cnn1_slvt.yaml`、`cnn1_lwt.yaml`、`cnn1_3conv_slvt.yaml`、`cnn2_slvt.yaml` 中的 `sigma_noise` 改为 `0.0001`。

- [ ] **Step 2: Commit**

```bash
git add configs/*.yaml
git commit -m "config: unify sigma_noise to 0.0001"
```

---

### Task 7: SLVTTrainer 支持 resume

**Files:**
- Modify: `mapping_network/trainer/slvt.py`
- Test: `tests/test_slvt.py`

**Interfaces:**
- `save_checkpoint` 保存 `optimizer_state_dict`、`scheduler_state_dict`、`best_test_acc`。
- 新增 `load_checkpoint(path)` 方法。
- `train()` 支持可选 `start_epoch`、`resume_results`、`resume_best_test_acc` 参数，或提供 `resume_from` 路径。

- [ ] **Step 1: 写失败测试**

```python
def test_slvt_trainer_resume(tmp_path, device):
    target_net = CNN2().to(device)
    mapping = LinearMappingNetwork(target_net.get_total_params(), 64, device=device)
    loss_fn = MappingLoss().to(device)
    loader = make_one_batch_loader(device)
    trainer = SLVTTrainer(
        mapping, target_net, loss_fn, loader, loader,
        epochs=2, device=device, checkpoint_dir=str(tmp_path),
        experiment_name='test_resume',
        save_interval=0,
    )
    trainer.train()
    ckpt_path = tmp_path / 'test_resume_final.pth'
    # 模拟 resume：新建 trainer 并从 checkpoint 恢复
    trainer2 = SLVTTrainer(
        mapping, target_net, loss_fn, loader, loader,
        epochs=2, device=device, checkpoint_dir=str(tmp_path),
        experiment_name='test_resume2',
        save_interval=0,
    )
    epoch = trainer2.load_checkpoint(ckpt_path)
    assert epoch == 2
    assert len(trainer2.results) == 2
```

- [ ] **Step 2: 运行测试确认失败**

Run: `uv run python3 -m pytest tests/test_slvt.py::test_slvt_trainer_resume -v`
Expected: FAIL

- [ ] **Step 3: 实现 SLVTTrainer resume**

修改 `save_checkpoint`：

```python
checkpoint = {
    ...
    'optimizer_state_dict': self.optimizer.state_dict(),
    'scheduler_state_dict': self.scheduler.state_dict(),
    'best_test_acc': self.best_test_acc,
    'results': results,
    'epoch': epoch if epoch is not None else self.epochs,
    'is_best': is_best,
}
```

新增 `load_checkpoint`：

```python
def load_checkpoint(self, path):
    checkpoint = torch.load(path, map_location=self.device, weights_only=False)
    self.mapping_net.load_state_dict(checkpoint['state_dict'])
    self.optimizer.load_state_dict(checkpoint['optimizer_state_dict'])
    self.scheduler.load_state_dict(checkpoint['scheduler_state_dict'])
    self.best_test_acc = checkpoint.get('best_test_acc', -1.0)
    self.results = checkpoint.get('results', [])
    return checkpoint.get('epoch', 0)
```

修改 `train` 支持 `start_epoch=1`：

```python
def train(self, start_epoch=1):
    ...
    for epoch in range(start_epoch, self.epochs + 1):
        ...
```

- [ ] **Step 4: 运行测试确认通过**

Run: `uv run python3 -m pytest tests/test_slvt.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add mapping_network/trainer/slvt.py tests/test_slvt.py
git commit -m "feat: support resume in SLVTTrainer"
```

---

### Task 8: LWTTrainer 支持 resume

**Files:**
- Modify: `mapping_network/trainer/lwt.py`
- Test: `tests/test_lwt.py`

**Interfaces：**
- 类似 SLVTTrainer，保存并恢复 `layer_mappings` 的 state_dict、optimizer、scheduler、results、best_test_acc。

- [ ] **Step 1: 写失败测试**

```python
def test_lwt_trainer_resume(tmp_path, device):
    target_net = CNN2().to(device)
    loss_fn = MappingLoss().to(device)
    loader = make_one_batch_loader(device)
    layer_gens = {
        name: {'type': 'linear', 'latent_dim': 16, 'alpha': 0.01}
        for name in target_net.get_group_names()
    }
    trainer = LWTTrainer(
        target_net, loss_fn, layer_gens, loader, loader,
        epochs=2, device=device, checkpoint_dir=str(tmp_path),
        experiment_name='test_lwt_resume',
        save_interval=0,
    )
    trainer.train()
    ckpt_path = tmp_path / 'test_lwt_resume_final.pth'
    trainer2 = LWTTrainer(
        target_net, loss_fn, layer_gens, loader, loader,
        epochs=2, device=device, checkpoint_dir=str(tmp_path),
        experiment_name='test_lwt_resume2',
        save_interval=0,
    )
    epoch = trainer2.load_checkpoint(ckpt_path)
    assert epoch == 2
    assert len(trainer2.results) == 2
```

- [ ] **Step 2: 运行测试确认失败**

Run: `uv run python3 -m pytest tests/test_lwt.py::test_lwt_trainer_resume -v`
Expected: FAIL

- [ ] **Step 3: 实现 LWTTrainer resume**

修改 `save_checkpoint` 增加 `optimizer_state_dict`、`scheduler_state_dict`、`best_test_acc`、`results`。

新增 `load_checkpoint`：

```python
def load_checkpoint(self, path):
    checkpoint = torch.load(path, map_location=self.device, weights_only=False)
    for name, state in checkpoint['state_dict'].items():
        self.layer_mappings[name].load_state_dict(state)
    self.optimizer.load_state_dict(checkpoint['optimizer_state_dict'])
    self.scheduler.load_state_dict(checkpoint['scheduler_state_dict'])
    self.best_test_acc = checkpoint.get('best_test_acc', -1.0)
    self.results = checkpoint.get('results', [])
    return checkpoint.get('epoch', 0)
```

修改 `train` 支持 `start_epoch=1`。

- [ ] **Step 4: 运行测试确认通过**

Run: `uv run python3 -m pytest tests/test_lwt.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add mapping_network/trainer/lwt.py tests/test_lwt.py
git commit -m "feat: support resume in LWTTrainer"
```

---

### Task 9: train.py 支持 --resume

**Files:**
- Modify: `mapping_network/scripts/train.py`

**Interfaces:**
- `python -m mapping_network.scripts.train --config ... --resume checkpoints/.../xxx.pth`

- [ ] **Step 1: 添加 --resume 参数与恢复逻辑**

```python
parser.add_argument('--resume', type=str, default=None)
...
if args.resume:
    start_epoch = trainer.load_checkpoint(args.resume) + 1
    results = trainer.results
else:
    start_epoch = 1
    results = []

results = trainer.train(start_epoch=start_epoch, resume_results=results)
```

需要调整 `train()` 签名：

```python
def train(self, start_epoch=1, resume_results=None):
    self.results = resume_results if resume_results is not None else []
    for epoch in range(start_epoch, self.epochs + 1):
        ...
```

- [ ] **Step 2: Commit**

```bash
git add mapping_network/scripts/train.py
git commit -m "feat: add --resume flag to train.py"
```

---

### Task 10: train_baseline.py 支持 --resume

**Files:**
- Modify: `mapping_network/scripts/train_baseline.py`

**Interfaces:**
- `python -m mapping_network.scripts.train_baseline --config ... --resume checkpoints/.../xxx.pth`

- [ ] **Step 1: 添加 --resume 参数**

```python
parser.add_argument('--resume', type=str, default=None)
```

- [ ] **Step 2: 保存 checkpoint 时增加恢复所需字段**

中间/最优/final checkpoint 增加 `optimizer_state_dict`、`scheduler_state_dict`、`best_test_acc`、`results`、`epoch`。

- [ ] **Step 3: 恢复训练逻辑**

在训练循环前：

```python
start_epoch = 1
results = []
best_test_acc = -1.0
if args.resume:
    ckpt = torch.load(args.resume, map_location=device, weights_only=False)
    model.load_state_dict(ckpt['state_dict'])
    optimizer.load_state_dict(ckpt['optimizer_state_dict'])
    scheduler.load_state_dict(ckpt['scheduler_state_dict'])
    start_epoch = ckpt.get('epoch', 0) + 1
    results = ckpt.get('results', [])
    best_test_acc = ckpt.get('best_test_acc', -1.0)
```

训练循环改为 `for epoch in range(start_epoch, epochs + 1)`。

- [ ] **Step 4: Commit**

```bash
git add mapping_network/scripts/train_baseline.py
git commit -m "feat: add --resume flag to train_baseline.py"
```

---

### Task 11: 全量测试与代码检查

**Files:**
- All modified files

- [ ] **Step 1: 运行全部测试**

Run: `uv run python3 -m pytest tests/ -v`
Expected: PASS（现有 35 个 + 新增测试）

- [ ] **Step 2: 运行 Ruff 检查**

Run: `uv run ruff check .`
Expected: PASS

- [ ] **Step 3: Commit**

```bash
git add .
git commit -m "test: verify issues 5/6/7/9 with full test suite"
```

---

## Self-Review

- Spec coverage：
  - #9 逐层 LRD：Task 1-3 覆盖。
  - #7 sigma_noise：Task 6 覆盖。
  - #6 lambda 初始化：Task 4-5 覆盖。
  - #5 resume：Task 7-10 覆盖。
- Placeholder scan：无 TBD/TODO，每步含具体代码。
- Type consistency：`load_checkpoint` 返回 epoch int；`train` 接收 `start_epoch` int 与 `resume_results` list；统一。
